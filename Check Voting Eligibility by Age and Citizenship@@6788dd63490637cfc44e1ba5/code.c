#include<stdio.h>
int main(){
    
    int age,cs;
    scanf("%d",&age);
    if(age >= 18 && cs == 0 ){
        printf("Eligible");
    }
    else {
        printf("Not Eligible");
    }
    
    return 0;
}